# Getting Started

Install the CLIA agent CLI locally.

## Install (from source)

1) Clone the repo

```bash
git clone https://github.com/clia-org/clia-agent-cli.git
cd clia-agent-cli
```

2) Build

```bash
swift build
```

## Next

- Run the CLI (once you know the command name):
  - `swift run clia --help`
- Learn the primitives:
  - <doc:architecture/triads>
  - <doc:architecture/s-type-system>
